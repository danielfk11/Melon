apiVersion: 1

datasources:
  - name: Prometheus
    uid: melonmq-prometheus
    type: prometheus
    access: proxy
    url: http://localhost:9091
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s
